pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Hello World build'
            }
        }
      stage('Deploy') {
            steps {
                echo 'Hello World deploy'
            }
        }
    }
}
